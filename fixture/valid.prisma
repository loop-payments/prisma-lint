generator client {
  provider        = "prisma-client-js"
  output          = "../generated/prisma/client"
}

datasource db {
  provider = "postgresql"
  url      = "fake-url"
}

model DbUser {
  id         String @id
  createdAt  String
  @@map(name: "user")
}

model DbTenant {
  /// prisma-lint-model-ignore required-fields createdAt
  id         String @id
  @@map(name: "tenant")
}
